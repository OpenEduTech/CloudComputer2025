# Learning Evaluation & Remediation Agent (Cloud-Native)

一套可容器化部署的学习评估系统：上传学习资料 →（可选）补充记忆点 → 自动出题/判卷 → 维护错题本与申诉流程。前端（Nginx 托管静态页）和 FastAPI 后端通过 `docker-compose` 编排，数据用 MongoDB/Redis 持久化。

---

## 目录

1. [系统架构概览](#系统架构概览)
2. [环境与依赖](#环境与依赖)
3. [环境配置指南](#环境配置指南)
4. [服务启动 & 验证](#服务启动--验证)
5. [常见问题排查](#常见问题排查)
6. [关键组件速览](#关键组件速览)
7. [后续拓展建议](#后续拓展建议)

---

## 系统架构概览

| **前端（frontend）**：`index.html` 提供学习资料/记忆点管理、测验、历史、错题等分页，使用 Nginx 托管静态页，并允许在 UI 内配置 API 地址。
| **API 服务（FastAPI）**：统一入口（`services/api/app/main.py`），暴露资料管理、记忆点、测验、判卷、错题本申诉等 REST 接口；文件上传依赖 `python-multipart`。
| **LLM 智能体层**：`agents.py` 内定义出题、判卷、错题申诉复核、记忆点自动生成，并新增错题定制化复习建议逻辑；提示词集中在 `prompt_templates.py`。
- **数据与缓存**：MongoDB 存储材料、记忆点、测验、提交记录、错题；Redis 缓存测验内容以减轻 DB 压力。
- **容器编排**：`docker-compose.yml` 同时运行 `frontend`、`api`、`mongo`、`redis` 四个服务。

---

## 环境与依赖

| 类型 | 组件 | 版本/说明 |
| --- | --- | --- |
| 语言运行时 | Python 3.11（API 镜像内置） | FastAPI、Pydantic、httpx 等依赖记录在 `services/api/requirements.txt` |
| 容器 | Docker Desktop（含 docker compose v2）| Windows/macOS/Linux 均可 |
| 数据服务 | MongoDB 7、Redis 7 | 由 compose 自动拉取官方镜像 |
| LLM 服务 | DeepSeek via SiliconFlow | 通过 `LLM_API_KEY`、`LLM_BASE_URL`、`LLM_MODEL` 配置 |
| 文件解析 | pypdf + openai-whisper | 需安装 `ffmpeg`；音频转写可通过 `WHISPER_MODEL` 指定模型；容器预装 CPU 版 PyTorch |
| 其他 | Node/TS 不必安装；前端为纯静态资源 | -- |

---

## 环境配置指南

### 1. 安装基础软件

1. **Docker Desktop**（建议最新版）
   - Windows 需启用 WSL2 或 Hyper-V。
   - 安装后打开 Settings → Resources，确认分配 ≥ 4GB 内存。
2. **Git（可选）**：方便拉取代码。

### 2. 克隆或下载项目

```bash
git clone https://example.com/Cloud_Final.git
cd Cloud_Final
```

### 3. 配置环境变量

1. 在项目根目录复制示例文件：
   ```bash
   copy .env.example .env  # Windows PowerShell/CMD
   ```
2. 根据需求编辑 `.env`（UTF-8）：

| 变量 | 默认值 | 说明 |
| --- | --- | --- |
| `MONGO_URI` | `mongodb://mongo:27017` | API 容器访问 Mongo 的地址（Compose 网络内） |
| `MONGO_DB` | `learning_agent` | 数据库名 |
| `REDIS_URL` | `redis://redis:6379/0` | Redis 连接串 |
| `LLM_API_KEY` | _必填_ | SiliconFlow/DeepSeek 的 API Key |
| `LLM_BASE_URL` | `https://api.siliconflow.cn/v1/chat/completions` | 可切换到自定义代理 |
| `LLM_MODEL` | `deepseek-ai/DeepSeek-V3.2-Exp` | 目标模型标识 |
| `LLM_TIMEOUT_SECONDS` | `45` | 单次 LLM 请求最大耗时，题目多时可调大 |
| `WHISPER_MODEL` | `small` | 音频转写模型标识，支持 `base`/`medium`/`large` |

> ✅ 未提供 `LLM_API_KEY` 时，系统会使用离线 stub 生成模板题目，但判卷质量受限。

### 4. Docker 镜像加速（可选）

若身处大陆网络，建议：

1. 在 Docker Desktop → Settings → Docker Engine 中配置：

   ```json
   {
     "registry-mirrors": ["https://mirror.ccs.tencentyun.com", "https://docker.mirrors.ustc.edu.cn"]
   }
   ```

2. API 镜像构建阶段会自动把 Debian APT 源切换为阿里云镜像，并提前从 `https://download.pytorch.org/whl/cpu` 安装 `torch==2.1.2`（CPU 版），避免重复拉取 CUDA 级别的大体积 wheel。若需替换镜像源或 Torch 版本，可编辑 `services/api/Dockerfile` 顶部的 `MIRROR`、`SEC_MIRROR`、`TORCH_INDEX_URL`、`TORCH_VERSION` 环境变量。

保存并重启 Docker Desktop 以生效。

---

## 服务启动 & 验证

### 1. 构建并启动

```bash
docker-compose up --build
```

首轮会拉取/构建四个镜像：`frontend`、`api`、`mongo`、`redis`。看到以下日志表示成功：

```
api_1      | INFO:     Uvicorn running on http://0.0.0.0:8000
frontend_1 | ... start worker processes
mongo_1    | ... waiting for connections on port 27017
redis_1    | ... Ready to accept connections
```

### 2. 打开前端与 API

- 前端：<http://localhost:3000>
  - Tab1: 学习资料+记忆点
  - Tab2: 测验与判卷
  - Tab3: 历史记录
  - Tab4: 错题与申诉
- API Swagger：<http://localhost:8000/docs>
- 健康检查：`curl http://localhost:8000/health` 应返回 `{"status":"ok"}`。

#### 资料录入两种方式

- **直接输入文本**：在前端“上传学习资料”卡片中粘贴内容并提交。
- **文件转文本**：同一位置选择 PDF / TXT / MP3 / WAV / M4A 等文件，点击“文件转文本并上传”，后端会使用 pypdf 解析文稿、使用 Whisper 模型转写录音，再将提取出的文本自动写入资料库并触发记忆点生成。

> 音频转写依赖 `ffmpeg` 与 `openai-whisper`。镜像已预装 `ffmpeg`，若自行运行 API，请确保本机亦已安装，并可通过 `WHISPER_MODEL` 指定模型（默认 `small`）。CPU 场景无需 GPU 驱动，Dockerfile 会抢先安装 CPU 版 `torch` 以缩短构建时间。

> ℹ️ 前端默认按照“当前域名 + 8000 端口”访问 API。如果部署在不同域名/端口，可在 `index.html` `<script>` 顶部提前声明：
> ```html
> <script>
>   window.__API_BASE__ = 'https://api.example.com';
> </script>
> ```
> 这样浏览器将直接调用指定地址。

### 3. 停止/后台运行

- 前台停止：在 `docker-compose up` 的终端按 `Ctrl+C`。
- 后台运行：`docker-compose up -d`
- 关闭并清理：`docker-compose down -v`（会删除 Mongo 数据，请谨慎）。

---

## 常见问题排查

| 症状 | 排查思路 |
| --- | --- |
| 浏览器提示 `Failed to fetch` | 确认 API 已启动；检查 `docker-compose logs api`；确保 CORS 中间件已加载（代码中已默认允许 `*`）。 |
| 端口冲突 | 查看 `netstat -ano | findstr :3000` / `:8000`，修改 `docker-compose.yml` 中映射端口再 `up --build`。 |
| Mongo/Redis 启动失败 | 可能是旧容器未清理：`docker-compose down -v` 后重启；或宿主机同端口已有服务。 |
| LLM 调用报 401 | 核对 `.env` 中 `LLM_API_KEY` 是否生效；可在 API 容器内 `echo $LLM_API_KEY` 调试。 |
| 题量 >2 时频繁落回默认题 | 查看 `docker-compose logs api` 是否有 `ReadTimeout`；通过 `.env` 设置 `LLM_TIMEOUT_SECONDS=60`，或暂时减小 `num_questions`。 |
| 上传文件提示 `Form data requires "python-multipart"` | 重新构建 API 镜像（依赖已写入 `requirements.txt`）；若本地调试请手动 `pip install python-multipart`。 |
| 上传资料报 `UnicodeEncodeError: surrogates not allowed` | 新版 `storage.py` 会自动清洗非法字符；若仍复现，确认 MongoDB 版本 ≥5 且输入为 UTF-8，可先在上传前自行清理文本。 |
| 构建镜像下载大量 `nvidia_*` wheel | 表示 Dockerfile 未命中 CPU Torch 层：确保未改动 `pip install` 顺序，或手动设置 `TORCH_INDEX_URL=https://download.pytorch.org/whl/cpu`。 |
| WSL `.vhdx` 体积过大 | 先 `docker system prune -af --volumes`，再 `wsl --shutdown` 后用 `Optimize-VHD` 或导出/导入压缩虚盘。 |
| 前端空白 | 清理浏览器缓存或使用隐身窗口；确认 `frontend` 容器日志无 404。 |

---

## 关键组件速览

| 路径 | 作用 |
| --- | --- |
| `frontend/index.html` | 单页应用，含分页导航、记忆点/测验/错题 UI 与 Fetch 调用 |
| `services/api/app/main.py` | FastAPI 路由、CORS、依赖注入、LLM 复核、申诉逻辑 |
| `services/api/app/agents.py` | 出题/判卷/申诉/记忆点生成 + 错题 AI 建议，支持 LLM 超时配置 |
| `services/api/app/storage.py` | Mongo/Redis 数据访问层，写入前自动过滤非法 Unicode |
| `services/api/app/models.py` | Pydantic 模型（Material、Memory、Quiz、Weakness 等） |
| `docker-compose.yml` | 一键拉起 frontend + api + mongo + redis |

API 示例（更多见 Swagger）：

- `POST /materials`：上传资料，若未填记忆点则由 AI 生成并持久化。
- `POST /materials/ingest`：上传 PDF/TXT/录音文件，自动提取文本后复用资料存储与记忆点生成流程。
- `GET /materials?user_id=...&keyword=...`：按关键词/主题检索资料。
- `POST /quizzes`、`POST /grade`：生成题目并判卷。
- `GET /quizzes/history`：查看测验历史与得分。
- `POST /weakness-book/appeal`：触发错题申诉，AI 重新核分。

任何问题可查看 `docker-compose logs -f`（可加 `api`/`frontend`/`mongo`/`redis` 过滤），或在 `services/api` 下运行 `uvicorn app.main:app --reload` 进行本地调试（需本机安装 Mongo/Redis 并调整连接串）。若 Docker Desktop 的 `.vhdx` 虚盘空间居高不下，可在清理镜像后执行 `wsl --shutdown` 并配合 `Optimize-VHD` / `wsl --export` → `--import` 压缩磁盘。祝使用愉快！
