# 知识图谱集成项目

## 📁 当前目录结构
```
C:\Users\86178\Desktop\云计算\大作业\backend\
├── agent-service/           # A同学的Agent服务
│   ├── api.py              # FastAPI接口 (POST /graph/build)
│   ├── agent_pipeline.py   # 图谱生成pipeline
│   ├── llm_client.py       # DeepSeek API调用
│   ├── step4_postprocess.py
│   ├── prompts/            # LLM提示词
│   ├── schema/             # 数据格式
│   ├── requirements.txt    # Python依赖
│   ├── .env.example
│   └── Dockerfile.agent    # 容器配置
├── app/                    # 我的后端服务
│   ├── main.py
│   ├── api/
│   │   ├── graph.py       # POST /api/graph/build
│   │   └── task.py        # GET /api/task/status
│   ├── core/
│   │   ├── config.py      # USE_MOCK配置
│   │   ├── db.py
│   │   └── schema.py
│   └── services/
│       └── graph_service.py  # 调用A的Agent服务
├── docker-compose.yml      # 4个服务：backend + agent-service + neo4j + redis
├── Dockerfile
├── requirements.txt        # 合并依赖
├── .env                    # 统一配置
├── test.html              # 网页测试界面
└── graph.schema.json
```

## 🚀 启动方式
```bash
# 1. 进入目录
cd C:\Users\86178\Desktop\云计算\大作业\backend

# 2. 设置API密钥（必须）
# 编辑 .env 文件，设置：DEEPSEEK_API_KEY=sk-xxxxxxx

# 3. 启动服务
docker-compose up -d --build

# 4. 测试服务
curl http://localhost:8001/healthz      # Agent服务
curl http://localhost:8000/health       # 后端服务
```

## ❌ 当前问题
**系统返回Mock数据，没有调用A的Agent服务**

### 现象：
- API返回 `"source": "mock-service"`
- 数据是随机生成的，不是AI生成的

### 检查点：
1. `.env` 中 `USE_MOCK=False`
2. `.env` 中 `DEEPSEEK_API_KEY` 有效
3. Agent服务正常：`curl http://localhost:8001/healthz`

### 需要A同学确认：
1. DeepSeek API密钥是否正确
2. Agent服务的 `/graph/build` 接口是否正常
3. requirements.txt 依赖是否完整