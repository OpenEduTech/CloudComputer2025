# 学习效果评估和巩固智能体 项目报告

## 成员信息
组员：周子轩

学号：10235501438

负责内容：全权负责

## 实验目的
1. 构建一套可容器化部署的学习评估系统，实现“资料上传 → 记忆点辅助 → 测验生成 → 判卷反馈 → 错题诊疗”闭环。
2. 支持 PDF/TXT/音频文件自动转文本录入，并联动记忆点生成与错题本维护。
3. 集成 LLM（DeepSeek via SiliconFlow）完成出题、判卷、错题复核与个性化复习建议，保障题目质量与反馈针对性。
4. 在有限网络环境下优化镜像构建速度、依赖体积与运行体验，确保开发者快速复现与调试。

## 实验环境
- **硬件**：Windows 11 + WSL2，8 核 CPU / 16 GB RAM（Docker Desktop 分配 4 GB）。
- **软件**：
  - Docker Desktop 4.x（Compose v2），MongoDB 7，Redis 7。
  - Python 3.11-slim（API 镜像基底），Nginx:alpine（前端）。
  - LLM：SiliconFlow DeepSeek-V3.2-Exp；音频转写依赖 openai-whisper + ffmpeg。
- **辅助工具**：Git、VS Code、浏览器（Chrome/Edge）

## 系统设计
1. **前端单页应用**：`frontend/index.html` 以卡片 + Tab 形式展示资料上传、记忆点、测验、历史、错题等功能，支持配置 API 基址与分页展示。
2. **FastAPI 服务**：`services/api/app/main.py` 暴露 REST 接口，涵盖资料 CRUD、文件转文本、记忆点、测验、判卷、申诉、错题管理等能力。
3. **智能体层**：`agents.py` 集中封装 LLM 调用：
   - 出题：优先在线调用 DeepSeek，失败时回退离线模板。
   - 判卷：基于重合度、MCQ 对比实现部分得分，并调用二次 QA 层校验。
   - 复习建议：对错题收集上下文，请 LLM 输出定制化“建议复习点”，若失败则用兜底规则。
   - 记忆点：从资料中抽取标题/内容/标签。
4. **数据层**：`storage.py` 通过 MongoDB/Redis 持久化材料、测验、错题、记忆点、提交历史；新增 UTF-8 清洗防止非法字符写入。
5. **文件 ingestion**：`ingestion.py` 判断上传类型，PDF 走 pypdf，音频调用 Whisper + ffmpeg 并清理临时文件。
6. **容器编排**：`docker-compose.yml` 同时拉起 frontend、api、mongo、redis，支持 `.env` 配置跨容器访问。

## 架构逻辑
```
┌────────────┐       ┌──────────┐       ┌────────────┐
│  前端 SPA  │ <--->    FastAPI   <--->   Mongo/Redis
└────────────┘       └────┬─────┘       └────┬───────┘
                          │                  │
                            调度/缓存           缓存 quiz
                          ▼                  ▼
                     ┌──────────────┐   ┌──────────────┐
                     │  LLM Agent   │   │ File Ingestor│
                     └────┬─────────┘   └────┬─────────┘
                          │                  │
                   在线 DeepSeek / 离线兜底   │
                                             │
                                         Whisper/pypdf
```
- 前端所有操作均通过 Fetch 调用 API，API 内部依赖 DataStore 与 Agent 协作。
- 判卷结果写入 Mongo，同时将错题写入 `weaknesses` 集合，并允许后续申诉与删改。
- Redis 缓存测验 JSON，避免重复序列化。

## 功能实现
1. **资料与记忆点**：支持文本录入与文件上传；记忆点可手动输入或由 LLM 自动生成，前端可检索/删除。
2. **测验生成**：根据资料内容与难度梯度请求 LLM；当 LLM 超时或失败时走 `_offline_generate` 以确保可用题目。
3. **判卷 + 部分得分**：MCQ 根据选项/字母匹配，主观题按字符重合度授予部分分，同时记录 rationale；错题将触发 `_enrich_feedback_with_ai` 向 LLM 请求针对性建议。
4. **错题本与申诉**：错题自动入库，可在前端查看并发起申诉；申诉再次调用 LLM 判断用户答案是否可接受。
5. **历史记录**：保存每次测验的答案、得分、反馈，供查询与再次复盘。
6. **文件转文本**：PDF 走 pypdf，音频走 Whisper；上传后统一进入 `_create_material`，保证记忆点逻辑一致。
7. **运维体验**：
   - Dockerfile 自动切换 APT 源到阿里云，设置 `TORCH_INDEX_URL` 为 CPU wheel，避免下载 CUDA 依赖。
   - 新增 `LLM_TIMEOUT_SECONDS` 控制在线题量大时的超时阈值。
   - `python-multipart`、UTF-8 清洗等依赖已写入 README 与 requirements。

## 项目中的障碍及解决方案
| 障碍 | 现象 | 解决方案 |
| --- | --- | --- |
| Debian 官方源 502 | `apt-get install ffmpeg` 失败 | Dockerfile 自动切换 APT 源，必要时生成新的 sources.list，并处理 deb822 `debian.sources` 文件。 |
| Whisper 拉取 GPU 依赖 | 构建时下载 2GB+ 的 `torch`+`nvidia_*` wheel | 在 Dockerfile 中预设 CPU Torch 镜像源 + 版本，并在安装顺序上先装 torch 再装其他依赖，彻底消除 CUDA 包。 |
| LLM 超时导致题目回退 | 题目数 >2 时常出现模板题 | `_call_llm` 改为可配置超时（默认 60s，可通过 `.env` 上调），并在 README 中提示调参方法。 |
| 表单上传报错 | FastAPI 报缺少 `python-multipart` | 将依赖加入 `requirements.txt` 并重建镜像，README 中明确表单上传所需依赖。 |
| Mongo `UnicodeEncodeError` | PDF/音频解析含代理字符 `\ud835` | `storage.py` 引入 `_sanitize_for_storage`，递归清理非法 Unicode，确保写库安全。 |
| Docker `.vhdx` 膨胀 | WSL 虚盘达 18GB+ | 文档中记录使用 `docker system prune` + `wsl --shutdown` + `Optimize-VHD` 或导出/导入压缩的方法。 |

## 实验结果
- 成功通过 Docker Compose 一键拉起前端、API、Mongo、Redis；前端页面可在本地访问并完成完整学习流程。
- 文件上传（PDF/音频）可正确解析成文本，记忆点自动生成成功率 >90%。
- 生成 4-10 题时，LLM 输出多样化题型，错题反馈提供个性化复习建议，评分结果记录在历史与错题本中。
- 通过 Env 调优后，镜像构建时间从 10+ 分钟降至约 4 分钟，下载体积减少至 <800 MB。

## 实验亮点
1. **容器内文件转文本一体化**：PDF 与录音统一入口，用户无需外部工具即可快速录入学习资料。
2. **错题 AI 建议**：结合题干、难度、用户答案，自动生成针对性复习提示，大幅提升反馈价值。
3. **可配超时 + 离线兜底**：保证 LLM 差错或网络抖动时仍能给出题目与判卷，不阻断学习流程。
4. **工程化优化**：APT 源、PyTorch CPU wheel、pip 镜像、UTF-8 清洗等措施显著提升构建与运行稳定性。
5. **详尽文档**：README/实验报告覆盖环境、故障排查、VHDX 压缩等运维细节，方便后续交接。

## 实验结论
本实验完成了一个端到端的学习评估系统，具备资料管理、AI 出题判卷、错题诊疗、文件转文本等功能，并针对国内网络环境与大型依赖做了系统性优化。通过引入 LLM 超时配置、离线兜底与错题个性化建议，系统在可靠性与教学价值方面均取得平衡。后续可在此基础上扩展多语言支持、细粒度权限控制以及更丰富的学习数据分析，以支撑更大规模的教学场景。
