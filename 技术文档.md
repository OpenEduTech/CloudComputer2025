## 1. 架构设计
### 1.1 系统架构概述
- **前端（Next.js/React）**：文件/URL 上传、状态展示（折叠卡片）、Markdown/PDF 导出（html2canvas + jsPDF + KaTeX），API 基址由 `NEXT_PUBLIC_API_BASE` 配置。
- **后端（FastAPI）**：入口 `/ppt/process`，支持本地文件或云端 URL；临时落盘后交给管线。
- **业务管线（PPTAgentPipeline）**：
  1. PPT 解析：抽取标题/正文/层级，过滤目录页/纯标题页；
  2. 语义聚合：继承标题、合并重复主题页；
  3. 向量检索：`embedding.py` 生成向量，`vector_store.py`（Faiss）做近邻检索；可替换云向量/企业知识库；
  4. LLM 扩写：并行调用大模型（DeepSeek/可配置），补充原理、公式推导、代码示例、延伸知识；带缓存与容错；
  5. 组装结果：输出 `topics`（title/section/raw_text/enrichment）与 `global_notes`。
- **静态资源**：背景图 `public/pic.webp`；PDF/Markdown 前端本地生成。

### 1.1.1 关键代码（部分）
- **前端上传/URL 入口** `frontend/app/page.tsx`
  ```tsx
  const [url, setUrl] = useState("");
  const handleUpload = async (e) => {
    e.preventDefault();
    if (!file && !url.trim()) { setMessage("请选择文件或URL"); return; }
    const formData = new FormData();
    if (file) formData.append("file", file);
    if (url.trim()) formData.append("url", url.trim());
    const resp = await axios.post(`${apiBase}/ppt/process`, formData, {
      headers: { "Content-Type": "multipart/form-data" },
    });
    setTopics(resp.data.topics); setGlobalNotes(resp.data.global_notes);
  };
  ```
- **后端入口** `backend/routers/ppt.py`
  ```py
  @router.post("/process", response_model=ProcessResponse)
  async def process_ppt(file: UploadFile | None = File(None),
                       url: str | None = Form(None)):
      if not file and not url: raise HTTPException(400, "file 或 url 至少提供一个")
      if file:
          with tempfile.NamedTemporaryFile(...) as tmp:
              tmp.write(await file.read()); temp_path = Path(tmp.name)
      else:
          resp = requests.get(url, timeout=30); resp.raise_for_status()
          with tempfile.NamedTemporaryFile(suffix=Path(url).suffix or ".pptx") as tmp:
              tmp.write(resp.content); temp_path = Path(tmp.name)
      topics, global_notes = PPTAgentPipeline().run(temp_path)
      return ProcessResponse(slides=[], topics=topics, global_notes=global_notes)
  ```
- **管线核心** `backend/services/pipeline.py`（示意）
  ```py
  def run(self, ppt_path: Path):
      pages = self.parser.parse(ppt_path)           # 抽取标题/正文/层级
      merged = self.grouper.merge(pages)            # 合并重复主题，继承标题
      texts = [m.text for m in merged]
      vectors = embed_texts(texts)                  # embedding.py
      neighbors = self.vector_store.search(vectors) # vector_store.py (Faiss)
      enriched = self.llm_expand(merged, neighbors) # 并行 LLM 扩写 + 缓存
      return self.build_topics(enriched)            # 组装 topics/global_notes
  ```
- **PDF 渲染关键** `frontend/lib/download.ts`
  ```ts
  marked.setOptions({ gfm: true, breaks: true });
  const mdToHtml = (md) => marked.parse(normalizeMath(md));
  const renderMathHtml = (html) =>
    html.replace(/\$\$([\s\S]+?)\$\$/g, (_, expr) => katex.renderToString(expr, { displayMode: true }))
        .replace(/\$([^\$\n]+?)\$/g, (_, expr) => katex.renderToString(expr, { displayMode: false }));
  ```

### 1.2 数据流向
1. 前端选择 PPTX 或输入云端 URL → 提交 multipart 至 `/ppt/process`。（代码：`app/page.tsx` 表单与提交）
2. 后端若为 URL 则下载，否则直接保存临时文件 → 调用 Pipeline。（代码：`routers/ppt.py` 文件/URL 分支、临时落盘）
3. Pipeline 解析/聚合 → 向量检索（本地 Faiss / 外部知识源）→ 并行 LLM 扩写 → 返回 JSON。（代码：`services/pipeline.py` 调用 `embedding.py`、`vector_store.py`）
4. 前端展示为折叠卡片，支持 Markdown/PDF 导出。（代码：`components/SlideCard.tsx` 渲染；`lib/download.ts` 导出）

### 1.3 云原生与组件
- **容器化**：后端 Dockerfile，前端可同样封装；支持 `docker compose up --build` 一键启动。（关键文件：`backend/Dockerfile`，可选 `docker-compose.yml`）
- **可扩展点**：Faiss 可换成云向量服务（如 Milvus/ES 向量插件），LLM 调用可换成自建网关或 Serverless 推理。（代码挂点：`vector_store.py` 保持接口，`services/pipeline.py` 调用处替换实现）
- **部署**：
  - 单机/小规模：Docker Compose。
  - 缓存/队列：Redis 作为 LLM 结果缓存与任务队列；Ingress 暴露 API。

## 2. 分工说明（评分按照组内统一分数评分）
- **宋昶郴（前端 50%）**
  - 界面/交互：`app/page.tsx`（上传/URL、状态流转、展示）。
  - 导出：`lib/download.ts`（Markdown/PDF 渲染与 KaTeX 公式）。
  - 样式与资源：`app/globals.css`、背景 `public/pic.webp`，UI 细节与体验。
- **严闻杰（后端 50%）**
  - API：`routers/ppt.py` 文件/URL 处理、异常/校验。
  - 管线：`services/pipeline.py` 解析/聚合/检索/扩写；`embedding.py`、`vector_store.py`。
  - 部署/配置：`backend/Dockerfile`、`requirements.txt`、`.env.example`，模型/检索接入配置。

## 3. 智能体策略
### 3.1 关键 Prompt 模板

指令：你是 PPT 学习笔记助手，需要：
- 识别 PPT 层级（章节/标题/子标题/正文/图片描述）。
- 合并多页同主题内容，去重冗余。
- 输出结构化笔记：
  1) 概要（3-5 句，聚合本主题页与关联页）
  2) 核心要点：公式/原理/推导；必要时给简洁代码示例（Python）
  3) 延伸/关联：相关概念、对比、阅读材料
- 避免逐页重复，标题页/目录页不展开内容。
输入：
- 当前主题聚合文本（含标题、正文、图片文字）
- 近邻检索片段（可为空）
输出：Markdown，使用合理标题/列表，公式用 `$...$` 或 `$$...$$`，代码用 ```python```
（Prompt 构建挂点：`services/pipeline.py` 中 prompt_builder.build 传入 page_text 与 neighbors）

### 3.2 LLM Agent 设计
- **角色**：PPT 学习笔记助手。
- **上下文**：聚合后的主题文本 + 近邻检索结果。
- **工具链**：
  - 文本拆页与聚合：PPT 解析器 + 标题继承规则。
  - 向量检索：Faiss（可替换云向量）；结果注入 Prompt。
  - 并行调用：对每个主题并发请求大模型；缓存避免重复调用。
- **容错与优化**：
  - 超时/失败重试；空检索时提示“无外部参考”。
  - 规范化公式分隔符，前端/导出用 KaTeX 渲染。
  - 去重与截断：对重复主题合并、控制段落长度。

#### 3.2.1 关键代码钩子
- **检索注入**（`services/pipeline.py`）
  ```py
  neighbors = self.vector_store.search(vectors, top_k=5)
  prompt_ctx = {"page_text": merged[i].text, "neighbors": neighbors[i]}
  llm_input = self.prompt_builder.build(prompt_ctx)
  ```
- **并行 LLM 扩写**
  ```py
  with ThreadPoolExecutor(max_workers=8) as ex:
      futures = [ex.submit(self.llm.generate, p) for p in prompts]
      results = [f.result() for f in futures]
  ```
- **公式规范化与渲染**（前端）
  - `components/SlideCard.tsx`：normalizeMath 统一 `\(...\)/\[...\]` → `$...$/$$...$$`，便于 remark-math/KaTeX。
  - `lib/download.ts`：marked 渲染 + KaTeX renderToString，保证 PDF 内公式正常。

### 3.3 可替换/扩展
- **模型**：DeepSeek 可替换为 OpenAI/自建推理服务；保持统一调用接口。
- **检索**：接入企业知识库/学术 API（Arxiv 等），在 Prompt 中标注来源。
- **输出格式**：已用 Markdown，保留代码块与公式以便渲染/导出。


