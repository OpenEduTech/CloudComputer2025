# 云计算期末大作业 技术文档

## 项目三：跨学科知识图谱智能体

### 云原生工程负责人：孟航平
- 学号：10205501421
- 主要负责模块：前后端开发、容器化部署、可视化
- 具体任务：
  - 用 FastAPI 开发后端 API，对接 Agent 和图数据库
  - 用 D3.js/ECharts 实现前端可视化页面
  - 编写 Dockerfile 和 docker-compose.yml，完成容器化配置
  - 部署测试，确保服务稳定运行
  - 录制demo，展示核心功能

### 智能体开发负责人：潘锐辰
- 学号：10235304449
- 主要负责模块：LLM Agent、知识图谱构建、校验层
- 具体任务：
  - 设计 Prompt 模板，实现跨学科概念关联挖掘
  - 用 LangChain 开发 Agent 工具链，集成搜索功能
  - 定义图谱 JSON 数据结构，将 Agent 输出转换为图谱格式
  - 与图数据库交互，完成数据写入与查询操作

---

## 一、项目总体说明

本项目实现了一个跨学科知识图谱智能体系统。系统以用户输入的“核心概念”为起点，利用大语言模型进行跨学科知识挖掘，自动构建多跳知识图谱，并通过 Web 前端进行可视化展示，同时支持将生成的知识图谱写入 Neo4j 图数据库。

系统整体采用云原生思想进行设计，基于 Docker Compose 实现多服务容器化部署，具备良好的可复现性与工程完整性。

---

## 二、系统架构设计（Architecture Design）

### 2.1 总体架构

系统采用三层架构设计：

1. **前端层（Web Frontend）**
2. **后端层（REST API + 智能体）**
3. **数据存储层（Neo4j 图数据库）**

各层通过 HTTP 与 Bolt 协议进行通信，并通过 Docker Compose 进行统一编排与部署。

**对应文件：**
- docker-compose.yml
- backend/Dockerfile
- frontend/index.html

---

### 2.2 前端层设计

前端负责用户交互与知识图谱可视化。

**主要功能：**
- 接收用户输入的核心概念
- 调用后端接口触发知识图谱生成
- 以力导向图方式展示节点与边
- 按学科类别对节点进行颜色区分
- 展示节点数量与边数量等统计信息

**对应文件：**
- frontend/index.html
- frontend/script.js

**关键函数（script.js）：**
- `run()`：前端主入口函数，点击“生成图谱”按钮触发
- `fetch("/api/graph")`：调用后端 REST API
- `renderGraph(nodes, edges)`：渲染知识图谱
- `showLoading() / hideLoading()`：加载动画控制

---

### 2.3 后端层设计

后端基于 FastAPI 实现，负责智能体调度与业务流程控制。

**主要功能：**
- 接收前端请求
- 调用智能体生成跨学科知识图谱
- 解析与规范化模型输出
- 可选地将结果写入 Neo4j
- 向前端返回结构化 JSON 数据

**对应文件：**
- app.py
- agent.py
- prompt_templates.py
- graph_parser.py
- neo4j_client.py

**核心接口：**
- `GET /api/graph`

**对应函数（app.py）：**
- `api_graph(concept: str, write_neo4j: bool)`

该函数完成以下流程：
1. 调用 `agent.generate_graph(concept)`
2. 根据参数决定是否写入 Neo4j
3. 返回标准化图谱数据

---

### 2.4 图数据库层设计（Neo4j）

系统使用 Neo4j 作为图数据库，用于持久化存储生成的知识图谱。

**主要功能：**
- 存储概念节点及其学科属性
- 存储概念之间的有向关系
- 支持通过 Neo4j Browser 进行交互式查看

**对应文件：**
- neo4j_client.py
- docker-compose.yml

**核心类：**
- LocalNeo4jClient

**关键方法（neo4j_client.py）：**
- `__init__()`：初始化 Neo4j 驱动并验证连接
- `clear_local_data()`：清空已有图数据
- `import_graph(graph)`：写入节点与关系
- `close()`：关闭数据库连接

---

### 2.5 容器化与部署设计

系统采用 Docker 进行容器化，并使用 Docker Compose 进行服务编排。

**特点：**
- 后端服务与 Neo4j 独立容器运行
- 使用 .env 文件统一管理环境变量
- 支持一条命令完成部署

**对应文件：**
- docker-compose.yml
- backend/Dockerfile
- .env

**端口映射：**
- 后端与前端接口：8000
- Neo4j HTTP 接口：17474
- Neo4j Bolt 接口：17687

---

## 三、智能体策略设计（Agent Strategy）

### 3.1 智能体设计目标

智能体的设计目标并非简单的关键词扩展，而是实现真正意义上的“跨学科知识重组与结构化表达”。

**核心目标：**
- 强制跨学科覆盖
- 构建多跳知识关联
- 发现学科之间的桥梁概念
- 输出结构化、可解释的图谱数据

---

### 3.2 Prompt 工程设计

智能体通过精心设计的 Prompt 来约束大语言模型的输出行为。

**对应文件：**
- prompt_templates.py

**核心 Prompt：**
- MINING_GRAPH_PROMPT
- REFINE_GRAPH_PROMPT

**对应函数：**
- `get_mining_graph_prompt(concept: str)`

**Prompt 设计特点：**
- 明确要求输出 JSON 格式
- 强制覆盖多个学科领域
- 要求生成跨学科桥梁关系
- 支持多跳（一级、二级）概念扩展

---

### 3.3 智能体执行流程

智能体执行逻辑主要实现在 agent.py 中。

**对应文件：**
- agent.py

**核心类：**
- KnowledgeGraphAgent

**关键方法：**
- `generate_graph(concept)`：生成完整知识图谱
- `_llm_invoke_json(prompt)`：调用大模型并解析 JSON 输出
- `_refine_graph(graph)`：对图谱进行进一步规范与补充

**执行流程：**
1. 生成跨学科一级概念
2. 扩展二级关联概念
3. 构建学科间桥梁关系
4. 输出结构化图谱数据

---

### 3.4 图谱解析与规范化

为保证系统稳定性，模型输出需经过解析与规范化处理。

**对应文件：**
- graph_parser.py

**核心类：**
- GraphParser

**关键方法：**
- `parse(raw_graph, core_concept)`

**主要职责：**
- 校验 JSON 结构合法性
- 统一节点 ID 与属性格式
- 去重节点与边
- 转换为前端可直接使用的数据结构

**输出结构：**
- `nodes`：节点列表（包含学科与层级信息）
- `edges`：边列表（包含关系方向）

---

### 3.5 稳定性与工程鲁棒性设计

系统在设计中考虑了多种工程级异常情况：

- 使用自定义填充函数避免 Prompt 中 JSON 花括号冲突
- Neo4j 写入失败不影响图谱生成结果
- 前后端职责清晰分离
- 容器化部署保证环境一致性

这些设计有效提升了系统在真实云计算环境中的稳定性。

---

## 四、总结

本项目完整实现了一个基于云计算架构的跨学科知识图谱智能体系统，在系统架构、智能体设计、工程实现和可视化展示等方面均满足实验文档中对项目三的要求。
